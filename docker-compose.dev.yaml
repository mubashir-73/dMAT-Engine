services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: dmat
      POSTGRES_PASSWORD: dmat
      POSTGRES_DB: dmat
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
